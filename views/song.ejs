<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/setup.ejs')%>
<body>
    <%- include('./partials/navbar.ejs')%>
    <button onclick="history.back()">Go Back</button>
    <form action="/" method="POST">
        <button class='changeSong' type="submit" name='song' value='<%=song.mp3%>'>
            <img src="" alt="Album Cover">
        </button>
    </form>
    <h3><%=song.title%></h3>
    <table>
        <tr>
            <td>Artist:</td>
            <td>
                <% for(let i=0; i<song.artist.length;i++){ %>
                    <a href="/<%=song.artist[i]%>/"><%=song.artist[i]%></a>
                <% } %>
            </td>
        </tr>
        <tr>
            <td>Album:</td>
            <td>
                <a href="/<%=song.artist[0]%>/<%=song.album%>"><%=song.album%></a>
            </td>
        </tr>
        <tr>
            <td>Year:</td>
            <td><%=song.year%></td>
        </tr>
        <tr>
            <td>Track N°:</td>
            <td><%=song.songNum%></td>
        </tr>
        <tr>
            <td>Genre:</td>
            <td><%=song.genre%></td>
        </tr>
    </table>
    <h4>Upcoming Events of <%=song.genre%> Music near you</h4>
    <div>
        <% for(let i=0; i<2;i++){ %>
            <div>
                <h5>Event Name</h5>
                <img src="" alt="Artist Img">
                <p>Event information</p>
                <div class="Google maps"></div>
            </div>
        <% }%>
    </div>
    <% for(let i=0; i<song.artist.length;i++){ %>
        <h4>Upcoming Events featuring <%=song.artist[i]%></h4>
        <div>
            <% for(let i=0; i<2;i++){ %>
                <div>
                    <h5>Event Name</h5>
                    <img src="" alt="Artist Img">
                    <p>Event information</p>
                    <div class="Google maps"></div>
                </div>
            <% }%>
        </div>
    <% } %>
<script type="module" src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.esm.js"></script>
<script nomodule src="https://unpkg.com/ionicons@5.5.2/dist/ionicons/ionicons.js"></script>
</body>
</html>